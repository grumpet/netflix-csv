# Netflix Data Analysis

This Python script analyzes a dataset of Netflix titles. It provides insights into the distribution of movies by country and the most common cast members.

## How to Use

1. Ensure that you have the required Python libraries installed. You can install them using pip:

```bash
pip install numpy pandas matplotlib
